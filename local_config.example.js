// Copy this file to `local_config.js` and set your real keys there.
// Do NOT commit `local_config.js` to version control if it contains secrets.

// Example: window.FINNHUB_API_KEY = 'your_api_key_here'
window.FINNHUB_API_KEY = ''

// Optional admin unlock hash (SHA-256 hex) used by admin UI
// window.ADMIN_HASH = ''
